name: test_pipeline

source:
  source_name: dummy
  stream_name: creatures
  authentication:
    type: api_key
    params:
      token: dummy_key

destination:
  name: logger
  config:
    dummy: dummy

engine:
  backend:
    type: sqlite
    config:
      syncCursorInDBEvery: 2
      database: bizon
      schema: public
